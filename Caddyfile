# PawMesh API — HTTPS 리버스 프록시 (Caddy)
# Let's Encrypt 인증서를 자동 발급/갱신하며 Spring Boot(app:8080)로 프록시한다.
# API_DOMAIN / ACME_EMAIL 은 docker-compose 의 환경변수로 주입된다.

{
	email {$ACME_EMAIL}
}

{$API_DOMAIN} {
	# 모든 요청을 내부 app 컨테이너(8080)로 프록시. HTTP→HTTPS 리다이렉트는 Caddy 가 자동 처리.
	reverse_proxy app:8080
}
